/**
 * Visit Sri Lanka - Runtime Configuration
 * 
 * When deploying the frontend to cPanel File Manager and the backend to Vercel,
 * you can set your Vercel backend URL below:
 * 
 * Example:
 * window.__API_URL__ = 'https://your-project.vercel.app/api';
 * 
 * If left empty (''), it will use the build-time VITE_API_URL or '/api' as default.
 */
window.__API_URL__ = '';
